{
  "name": "@saadbutt/toolgate-confirm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Frozen-argument confirmation for AI agent tool calls. Zero runtime dependencies.",
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "demo": "node --experimental-strip-types src/demo.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "license": "MIT"
}
